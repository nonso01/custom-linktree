import * as _u from "./util.js"

const DP = new Image()

const MAIN = new Promise((resolve, reject) => {
  
  let o = {
    a: 4, b: 7
  }
  
  _u.dom(_u.o, _u.dq("#root"))
  
}).catch(error => console.warn(error))

